- content_for :panel_head do
  %h1 Editing Committee
  = render partial: "shared/actions/edit_actions", locals: { allPath: committees_path, fieldName: 'Committee', showPath:  @committee}
  - if @committee.profile
    = link_to raw("<i class='glyphicon glyphicon-pencil'></i> Edit Profile"), edit_profile_path(@committee.profile), :class => 'btn btn-default'
= render 'form'
