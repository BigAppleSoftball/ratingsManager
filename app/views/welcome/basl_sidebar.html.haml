%link{:href => "https://fonts.googleapis.com/css?family=Montserrat", :rel => "stylesheet", :type => "text/css"}
=stylesheet_link_tag "http://#{request.host_with_port}/css/sponsorCarousel.css"
=link_to 'BASL Login', 'https://go.teamsnap.com/login/signin', {:target => '_blank', :class => 'teamsnap-login-btn', :title => 'Log into your teamsnap account'}
.basl-hotline-container
  Wondering if fields are open?
  %br
  Call the BASL Hotline
  %div.basl-hotline-number
    (212) 696-7327
- if @parkStatus
  .game-status{:class => "status-#{@parkStatus}"}
    = render partial: "parks/status_indicator", locals: {statusValue: @parkStatus , statusString: @parkStatus}
    - if @parkStatus == 1
      Some Parks are Partially Closed.
    - elsif @parkStatus == 2
      All Park are Closed
    - elsif @parkStatus == 0
      All Parks are open
.sponsors-carousel-wrapper
  .sponsors-carousel-window
    %ul.sponsors-carousel.js-sponsors-carousel
      -@sponsors.each do |sponsor|
        %li
          %a{:href=>sponsor.url, :title => sponsor.name, :target=> '_blank'}
            %img{:src=>"#{sponsor.logo_url}"}

= javascript_include_tag "https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"
= javascript_include_tag "http://#{request.host_with_port}/javascripts/sponsorsCarousel.js"