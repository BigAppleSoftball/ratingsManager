- content_for :panel_head do
  %h1 Request Reset Password Email

.row
  .col-md-6.col-md-offset-3
    = form_for(:password_reset, url: new_password_reset_path,:html => {:class => 'form-horizontal'}) do |f|
      .form-group
        = f.label :email, {:class => 'col-sm-2 control-label'}
        .col-sm-10= f.email_field :email, class: 'form-control'
      .form-group
        .col-sm-offset-2.col-sm-10= f.submit "Submit", class: "btn btn-primary"