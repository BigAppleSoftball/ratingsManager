%td{:align => 'center', :bgcolor => col_colors[:none]}
  %b 0
- count = 0
- (1..questions[:throwing]).each do |i|
  - count+= 1
  %td{:bgcolor => col_colors[:none],:align => 'center'}
    0
- (1..questions[:fielding]).each do |i|
  - count+= 1
  %td{:bgcolor => col_colors[:none], :align => 'center'}
    0
- (1..questions[:running]).each do |i|
  - count += 1
  %td{:bgcolor => col_colors[:none], :align => 'center'}
    0
- (1..questions[:hitting]).each do |i|
  - count += 1
  %td{:bgcolor => col_colors[:none], :align => 'center'}
    0