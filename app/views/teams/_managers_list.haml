.team-manager-container
  %b Managers:
  %ul.team-managers-list.list-unstyled
    - if team.managers.length > 0
      - team.managers.each do |manager|
        -if manager.profile
          %li=link_to manager.profile.name, manager.profile
    - else
      .no-manager No Team Manager