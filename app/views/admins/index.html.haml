

.panel.panel-danger
  .panel-heading
    %h1 Application Admins
    = link_to '+ Add New Admin', new_admin_path, :class =>'btn btn-primary'
    %br
    .well
      These are users that will have access to the ability to edit, create and delete sponsors, edit the hall of fame and other administrative duties
  .panel-body
    %table.table.table-striped.table-bordered
      %tr
        %th Email
        %th
        %th

      - @admins.each do |admin|
        %tr
          %td= link_to admin.email, admin
          %td= link_to 'Edit', edit_admin_path(admin), :class =>'btn btn-default'
          %td= link_to 'Destroy', admin, :method => :delete, :class =>'btn btn-danger', :data => { :confirm => 'Are you sure?' }

    %br
